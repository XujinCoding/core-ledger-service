<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coreledger.mapper.ProductCategoryMapper">

    <!-- 递归查询指定分类及其所有子分类的ID -->
    <select id="findAllCategoryIdsRecursive" resultType="java.lang.Long">
        WITH RECURSIVE category_tree AS (
            SELECT id FROM product_category WHERE id = #{categoryId} AND status = #{status}
            UNION ALL
            SELECT c.id FROM product_category c
            INNER JOIN category_tree ct ON c.parent_id = ct.id
            WHERE c.status = #{status}
        )
        SELECT id FROM category_tree
    </select>

</mapper>
